#!/bin/bash

function findAndRemove() {
for     dir in $(find -maxdepth 2 -type d ! -wholename '.') ; do
    echo $dir
    dirNoDash=${dir//-}
    dateTillDay=${dirNoDash:2:8}
#    if [[ $(date -d "$dateTillDay" +%s) -lt $(date -d "$daysToRemain days ago" +%s) ]] ; then
#        #rm -rf $dir
#        echo $dir
#    fi
done
    
}

dst='/mnt/mSATA/data/backups'
daysToRemain=7
cd $dst
